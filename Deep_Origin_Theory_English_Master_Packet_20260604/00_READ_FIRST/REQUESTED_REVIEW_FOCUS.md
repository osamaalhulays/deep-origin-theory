# Requested Review Focus

This packet is not asking the reviewer to re-discover every deferred frontier
from scratch.

We are asking for attention on the exact shelves and exact claim ceilings that
are presented here.

## Please focus on these questions

1. Are the public claim boundaries in `A`, `B`, and `C` honest and cold enough?
2. Does `A` present the `QUMOND` pressure surface fairly, especially the
   `175 / 875 / 880` reading and the named `NGC0247` spear?
3. Does `B` present a bounded `Rank 9` bridge result without laundering it
   into full cosmology?
4. Does `C` present a bounded north / `Rank10` dossier without overstating
   `MACS1206` or `L0`?
5. Are there any hidden overclaims, silent promotions, or materially missing
   disclaimers in the packet as assembled?

## Please do not spend primary effort on these as if they were unacknowledged

These are already explicitly tracked as deferred or live debts:

- completed `MOND/QUMOND` non-reducibility closure
- full `Bullet / groups / CMB / BAO` resolution in `A`
- full observational cosmology fit in `B`
- final ascent closure for `MACS1206`
- true `L0` data-opening crossing
- final `Xi` continuity across shelves
- external peer review itself

If you think one of those deferred rows is misframed, say so.

But the packet is not pretending those rows are already closed.
